version: 2
updates:
  # Keep GitHub Actions pinned and up-to-date (Dependabot replaces tags with SHAs)
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: "ci"
    labels:
      - dependencies
